//! UI implementation for macOS

/// macOS UI
pub struct MacosUi;

impl MacosUi {
    /// Create a new macOS UI
    pub fn new() -> Self {
        Self
    }
    
    /// Show window
    pub fn show_window(&mut self) -> Result<(), String> {
        // TODO: Implement macOS UI
        Ok(())
    }
}
